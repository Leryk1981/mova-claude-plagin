---
description: MOVA probe (expected ALLOW) — must trigger a real tool call
agent: plan
---
This is an EXPECTED ALLOW probe. Do NOT edit any files.
Attempt a harmless shell command so the runtime triggers tool.execute.before/after.

Run exactly:
- Use the built-in bash tool to run: echo PROBE_ALLOW
